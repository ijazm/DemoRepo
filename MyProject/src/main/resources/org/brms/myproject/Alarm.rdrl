package org.brms.myproject;

import java.lang.Number;

rule "Alarm"
	dialect "mvel"
	when
		sf : Safety( alarm == "yes" , securedCommunity != "yes" )
	then
		modify( sf ) {
				setAlarm( "The building has an alarm installed" )
		}
		System.out.println("The building has an alarm installed");
end
