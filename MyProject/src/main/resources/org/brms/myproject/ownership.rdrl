package org.brms.myproject;

import java.lang.Number;

rule "ownership"
	dialect "mvel"
	ruleflow-group "time"
	no-loop true
	when
		ot : Ownership( )
		exists (Ownership( timeOwnership < 3 ) and Ownership( timeOwnership != 0 )) 
	then
		modify( ot ) {
				setPriorAddress( "Display tab but not new purchase" )
		}
end
